[project]
name = "sandbox"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "lap>=0.5.12",
    "opencv-python>=4.11.0.86",
    "ultralytics>=8.3.241",
    "tqdm>=4.66.1", # Added tqdm for progress bar
]
